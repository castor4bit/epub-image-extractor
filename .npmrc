# Enforce strict lockfile usage
lockfile=true

# Enable dependency audit on install
audit=true

# Reject packages published less than 7 days ago
# Mitigates supply chain attacks — most malicious packages are
# detected and removed within a week of publication
minimum-release-age=7
